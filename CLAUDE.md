# CLAUDE.md

webcam-guard — a spare USB webcam as a room monitor (Windows only: continuous recording + phone viewer).

> **Read [PROJECT.md](PROJECT.md) end to end before working.** Design intent, what is still unverified, and the "do not touch" list live there.
> This file is a summary plus the commands. Where they disagree, PROJECT.md wins.
> [CHANGELOG.md](CHANGELOG.md) records reversed decisions — read it before re-proposing something that was already tried.

*[한국어는 아래에 ↓](#한국어)*

## The structure in one line

**One** ffmpeg process owns the webcam and uses the `tee` muxer to emit ① a 10-minute mp4 archive and ② an HLS live stream **at the same time**. Everything else — watchdog, cleanup, analysis, HTTP server, viewer — is Python standard library and inline HTML.

```
guard.py     the whole server (one file)    index.html   the phone viewer
.env         settings and secrets (gitignored). Template: .env.example
static/      vendored hls.js and fonts (generated by vendor.py — do not hand-edit)
events/      the logbook. Date-named JSON Lines (under --root)
tools/       development and verification, run without a camera. Not product code
```

**Verified (2026-08-23/24)**: recording, live view and browser playback all confirmed on a real webcam with ffmpeg 9.0. The one thing still unverified is **long-run stability**. Details in PROJECT.md section 2.

## Absolute rules

| Rule | Why |
|---|---|
| **Zero pip packages** | The user must finish with one line, `python guard.py`, on Windows. The moment there is a dependency, install difficulty jumps |
| **Exactly one ffmpeg process on the camera** | On Windows only one process can hold a webcam. "One for recording, one for streaming" is impossible. New features must go **inside** the existing pipeline |
| **tee targets are relative paths + `cwd=root`** | tee uses `:` as its own option separator, so an absolute `C:\...` path breaks on the drive colon |
| **The janitor never deletes the newest file** (`files[:-1]`) | The last file in the list is the one ffmpeg is writing right now |
| **Never put `onfail=ignore` on the segment output** | Live view may die; recording failing silently removes the entire point of a security camera. It belongs on the HLS side only |
| **Keep audio off (`-an`)** | Recording conversation carries legal weight that varies by region. If asked to enable it, raise that first |
| **`CLIP_RE` filename convention is fixed** | The janitor's sort, `/api/clips` parsing and `/clips/` path validation all depend on it simultaneously |
| **Two backslashes before the drawtext font colon** (`C\\:/...`) | One (`C\:/...`) makes ffmpeg **die outright, so nothing records**. Verified (PROJECT.md 5-8) |
| **An overlay must never kill recording** | If the font is missing, drop the filter and keep recording. Losing all footage to a decoration is the worst trade available |
| **Do not remove the `adopt_child(proc)` call** | It ties ffmpeg to a Windows Job Object. Without it a force-kill leaves ffmpeg orphaned, **holding the camera forever** (PROJECT.md 5-9) |
| **Raising retention means raising `--max-gb` too** | Whichever limit hits first wins. Raising only `--retain-days` changes nothing |
| **`/live/*` goes through `send_live()` only** | ffmpeg rewrites it every 2s. Streaming it with `send_file` gives PermissionError, length mismatches, and **a half-written playlist with no error at all** (PROJECT.md 5-11) |
| **Reconfigure stdout to UTF-8 at import** | Redirected output uses the legacy code page, where one non-ASCII log line **kills the process before it starts** |
| **The logbook reads `live/*.ts`** | Reopening the camera is impossible and clips lag 10 minutes. Reading files is unrelated to the one-ffmpeg rule (PROJECT.md 5-12) |
| **Motion is measured as percent of changed pixels** | Mean brightness difference dilutes small subjects and failed to detect a person in measurement (5-13) |
| **Compare faces with `faces > 0`** | It is `-1` when OpenCV is absent, so `if faces:` turns **every segment into a face event** (5-14) |
| **Viewer colours stay within the section 7 tokens** | The accent (`--sodium`) is for the record light, selection, timeline and gauges only. No new colours |

## Conventions

- **Documentation is English first, Korean below** in the same file. UI strings and error messages remain Korean.
- Code comments are English. Explain **why**, not what.
- **Always include the "why"** in answers to the user (an explicit preference).
- For real-device problems, **ask for the verbatim error text** rather than guessing. Whether a dshow failure is a device name, an unsupported resolution or a busy camera is only distinguishable from the message.

## Commands

```bash
python guard.py --list                     # find the camera name
python guard.py                            # run (settings come from .env)
python -m py_compile guard.py              # syntax check
python tools/selftest.py                   # full verification, no camera (54 items)
python tools/devserver.py                  # viewer work against a fake camera
python tools/calibrate.py                  # tune motion sensitivity for a room
python tools/vendor.py                     # fetch static/ assets (one-off)
```

## After changing code, always

1. `python -m py_compile guard.py`
2. `python tools/selftest.py` — covers the ffmpeg pipeline (synthetic `lavfi` input), every HTTP route, auth, Range, path traversal, config precedence and logbook scoring. Skips the pipeline part when ffmpeg is missing.
3. If you touched the viewer, look at it: `python tools/devserver.py`.
4. If you changed detection logic, **recalibrate against a real camera** (`tools/calibrate.py`). Synthetic video cannot tell you a room's noise floor — this project had two designs overturned exactly there.
5. If an `/api/*` response schema changed, change `index.html` **with it**. The viewer depends on that schema directly.

---
---

<a name="한국어"></a>

# CLAUDE.md (한국어)

webcam-guard — 남는 USB 웹캠으로 만드는 방 감시 카메라 (Windows 전용, 상시 녹화 + 휴대폰 뷰어).

> **작업 전에 [PROJECT.md](PROJECT.md)를 끝까지 읽을 것.** 설계 의도·미검증 목록·"손대면 안 되는 것"이 거기 있다.
> 이 파일은 그 요약과 실행 명령만 담는다. 충돌하면 PROJECT.md가 우선이다.
> [CHANGELOG.md](CHANGELOG.md)에는 되돌린 결정이 있다. 이미 시도된 걸 다시 제안하기 전에 확인할 것.

## 구조 한 줄 요약

ffmpeg 프로세스 **하나**가 웹캠을 잡고 `tee` 먹서로 ① 10분짜리 mp4 아카이브 ② HLS 라이브를 **동시에** 뱉는다. 나머지(감시·청소·분석·HTTP 서버·뷰어)는 전부 파이썬 표준 라이브러리와 인라인 HTML이다.

```
guard.py     서버 전체 (단일 파일)   index.html   휴대폰 뷰어
.env         설정·비밀번호 (gitignore 됨). 템플릿은 .env.example
static/      내장 hls.js·폰트 (vendor.py 생성물, 손으로 고치지 말 것)
events/      로그북. 날짜별 JSON Lines (--root 아래)
tools/       카메라 없이 돌리는 개발·검증 도구 (제품 코드 아님)
```

**검증 상태(2026-08-23/24)**: 실제 웹캠 + ffmpeg 9.0으로 녹화·라이브·브라우저 재생까지 확인됨. 남은 미검증은 **장시간 안정성**뿐이다. 자세한 건 PROJECT.md 2절.

## 절대 규칙

| 규칙 | 왜 |
|---|---|
| **pip 패키지 0개** | 사용자가 Windows에서 `python guard.py` 한 줄로 끝내야 한다. 의존성이 생기는 순간 설치 난이도가 급등한다 |
| **카메라를 잡는 ffmpeg은 하나뿐** | Windows에서 웹캠은 한 프로세스만 점유 가능. "녹화용 + 스트리밍용" 2개는 불가능하다. 새 기능은 기존 파이프라인 **안에** 넣어야 한다 |
| **tee 타깃은 상대 경로 + `cwd=root`** | tee는 `:`를 옵션 구분자로 쓴다. `C:\...` 절대 경로는 드라이브 콜론 때문에 깨진다 |
| **janitor는 최신 파일을 안 지운다** (`files[:-1]`) | 목록의 마지막 파일은 ffmpeg이 지금 쓰는 중이다 |
| **segment 출력에 `onfail=ignore` 금지** | 라이브는 죽어도 되지만 녹화가 조용히 실패하면 감시 카메라의 존재 이유가 사라진다. HLS 쪽에만 붙인다 |
| **오디오는 `-an` 유지** | 대화 녹음은 지역에 따라 법적 문제가 된다. 켜달라는 요청이 오면 이 점을 먼저 알릴 것 |
| **`CLIP_RE` 파일명 규약 고정** | janitor 정렬 · `/api/clips` 파싱 · `/clips/` 경로 검증 세 곳이 동시에 의존한다 |
| **drawtext 폰트 콜론은 백슬래시 2개** (`C\\:/...`) | 1개(`C\:/...`)면 ffmpeg이 **통째로 죽어 녹화가 안 된다**. 실측 확인됨 (PROJECT.md 5-8) |
| **오버레이가 녹화를 죽이면 안 된다** | 폰트를 못 찾으면 필터를 빼고 계속 녹화한다. 장식 때문에 영상 전부를 잃는 건 최악의 트레이드 |
| **`adopt_child(proc)` 호출을 빼지 말 것** | ffmpeg을 Windows Job Object에 묶는다. 빼면 강제 종료 시 ffmpeg이 고아로 남아 **카메라를 영구 점유**한다 (PROJECT.md 5-9) |
| **보관 기간을 늘리면 `--max-gb`도 같이** | 둘 중 먼저 걸리는 쪽이 이긴다. `--retain-days`만 올리면 실제로는 안 늘어난다 |
| **`/live/*`는 `send_live()`로만** | ffmpeg이 2초마다 다시 쓴다. `send_file`로 스트리밍하면 PermissionError·길이 불일치·**오류 없이 반쪽짜리 재생목록**이 난다 (PROJECT.md 5-11) |
| **stdout은 import 시점에 UTF-8로** | 리다이렉트된 출력은 레거시 코드페이지라 비ASCII 로그 한 줄에 **시작도 못 하고 죽는다** |
| **로그북은 `live/*.ts`를 읽는다** | 카메라 재점유는 불가, 클립은 10분 지연. 파일 읽기는 "ffmpeg 하나" 규칙과 무관하다 (PROJECT.md 5-12) |
| **모션은 변한 픽셀 비율로 잰다** | 평균 밝기차는 작은 피사체를 희석시켜 실측에서 사람을 못 잡았다 (5-13) |
| **얼굴 비교는 `faces > 0`** | OpenCV가 없으면 `-1`이라 `if faces:`로 바꾸면 **전부 얼굴 사건이 된다** (5-14) |
| **뷰어 색은 7번 섹션 토큰 안에서만** | 강조색(`--sodium`)은 녹화등·선택·타임라인·게이지에만. 새 색 추가 금지 |

## 소통 규약

- **문서는 영어를 먼저 쓰고 그 아래에 한국어**를 같은 파일에 잇는다. UI 문구와 에러 메시지는 한국어를 유지한다.
- 코드 주석은 영어. 무엇이 아니라 **왜**를 적는다.
- 사용자 답변에는 **항상 "왜"를 포함**한다 (명시적 선호).
- 실기기 문제는 **추측하지 말고 오류 메시지 원문을 요청**한다. dshow 실패 원인이 장치명인지, 해상도 미지원인지, 점유 충돌인지는 메시지로만 구분된다.

## 명령

```bash
python guard.py --list                     # 카메라 이름 찾기
python guard.py                            # 실행 (설정은 .env 에서)
python -m py_compile guard.py              # 문법 검사
python tools/selftest.py                   # 카메라 없이 전체 검증 (54개 항목)
python tools/devserver.py                  # 가짜 카메라로 뷰어 작업
python tools/calibrate.py                  # 방에 맞게 모션 감도 보정
python tools/vendor.py                     # static/ 자산 받기 (1회성)
```

## 코드를 고쳤으면 반드시

1. `python -m py_compile guard.py`
2. `python tools/selftest.py` — ffmpeg 파이프라인(lavfi 합성 입력) + HTTP 전 라우트 + 인증 + Range + 경로 탈출 + 설정 우선순위 + 로그북 채점까지 본다. ffmpeg이 없으면 파이프라인 부분만 건너뛴다.
3. 뷰어를 건드렸으면 `python tools/devserver.py`로 눈으로 확인.
4. 감지 로직을 고쳤으면 **실제 카메라로 재보정**(`tools/calibrate.py`). 합성 영상으로는 방의 노이즈 바닥을 알 수 없다 — 이 프로젝트에서 실제로 두 번 설계가 뒤집힌 지점이다.
5. `/api/*` 응답 스키마를 바꿨다면 `index.html`을 **같이** 고칠 것. 뷰어가 그 스키마에 직접 의존한다.
